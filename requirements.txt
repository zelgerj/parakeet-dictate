# Parakeet Dictation Tool — dependencies (macOS / Apple Silicon, Python 3.11+)
# ASR: NVIDIA Parakeet TDT v3 via MLX
parakeet-mlx        # loads mlx-community/parakeet-tdt-0.6b-v3
sounddevice         # microphone capture (16 kHz mono float32)
soundfile           # WAV read/write (used by the core-check script)
pynput              # global hotkey listener + simulated Cmd+V
pyperclip           # save/set/restore the clipboard
rumps               # menu-bar icon with status (optional)
# Request permissions actively via native macOS APIs (.app onboarding):
pyobjc-framework-AVFoundation         # Microphone (requestAccessForMediaType)
pyobjc-framework-Quartz               # Input Monitoring (CGRequestListenEventAccess)
pyobjc-framework-ApplicationServices  # Accessibility (AXIsProcessTrusted…)
